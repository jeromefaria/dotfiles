# Last.fm credentials for the mpv scrobbler.
# Copy this file to ~/.config/mpv/lastfm.env (which is gitignored) and fill
# in the two values below. scrobble.pl reads them at runtime.

LASTFM_USERNAME=
LASTFM_PASSWORD=
